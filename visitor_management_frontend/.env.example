# Visitor Management Frontend .env Example

# Backend API base URL (e.g., "http://localhost:8000" or the deployed backend URL)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# You may also add public configuration variables as needed for feature toggles, branding, etc.
